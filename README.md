# Twitter Status Image Generator
Image generator of a Twitter Status

Should work fine. Maybe some bugs somewhere. Who knows

Written for python 3+, requires pillow

´´´open cmd
pip install pillow
close cmd´´´
have fun

Add the path to helvetica and helvetica bold, ttf fonts work fine.
There is an example on how to use the function so I don't have to comment anything

